<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <TextView
            android:id="@+id/tv_change_fragment"
            style="@style/TextStyle"
            android:text="切换Fragment1"/>

        <TextView
            android:id="@+id/tv_change_fragment1"
            style="@style/TextStyle"
            android:text="切换Fragment2"/>

        <TextView
            android:id="@+id/tv_change_params"
            style="@style/TextStyle"
            android:text="配置params"/>

        <TextView
            android:id="@+id/tv_activity_send"
            style="@style/TextStyle"
            android:text="activity传值 setArgument"/>

        <TextView
            android:id="@+id/tv_activity_eventbus"
            style="@style/TextStyle"
            android:text="activity传值 EventBus"/>

        <FrameLayout
            android:id="@+id/fl_root"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="30dp"
            tools:context=".test.fragment.TestFragmentActivity"/>
    </LinearLayout>



</layout>