<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data class="CreateThreadBinding">

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".activity.ThreadCreateActivity">

        <TextView
            android:id="@+id/tv_thread_extend"
            style="@style/TextStyle"
            android:text="继承Thread"/>

        <TextView
            android:id="@+id/tv_thread_run"
            style="@style/TextStyle"
            android:text="实现runnable"/>

        <TextView
            android:id="@+id/tv_thread_callable"
            style="@style/TextStyle"
            android:text="使用callable"/>


    </LinearLayout>
</layout>